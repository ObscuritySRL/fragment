# CLion
.idea

# CMake
CMakeLists.txt.user
CMakeCache.txt
CMakeFiles
CMakeScripts
Testing
Makefile
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps
# Project exclude paths
/cmake-build-debug/
/cmake-build-release/

# Build output (build.bat / build_test.bat)
/build/
/build_log.txt
*.obj

# Python bytecode cache
__pycache__/
*.pyc

# Third-party libcurl test corpus: the BINARIES are fetched/copied locally and
# not committed (see test/curl/README.md to reproduce), but the README that
# documents how to obtain them IS tracked.
/test/curl/*.dll
/test/curl/*.exe

# Local build dirs used to mirror the CI (VS multi-config) layout.
/build_ci/

# Stray debug log from an injected target (configurable via FRAGMENT_LOG_FILE).
/Fragment.log
*.Fragment.log
